name: App

on:
  push:
    paths-ignore:
      - "app/**"
      - ".github/workflows/app.yml"

jobs:
  build:
    name: Build App
    runs-on: ubuntu-20.04

    steps:
      - run: 'echo "No checks required"'
