import { createTheme } from "@mui/material/styles";

export const metrixTheme = createTheme({
  palette: {
    primary: {
      main: "#c70039",
    },
    secondary: {
      main: "#2a7b9b",
    },
    text: {
      primary: "#444444",
    },
    background: {
      default: "#eddd53",
      paper: "#ffffff",
    },
  },
  typography: {
    fontFamily: "Rubik",
  },
});
